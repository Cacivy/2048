kind: pipeline
name: default

steps:
- name: build
  image: node
  commands:
  - npm install
  - git config --global user.email "609448234@qq.com"
  - git config --global user.name "cacivy"
  - npm run ci
  - npm run codecov
  - npm run deploy

trigger:
  branch:
  - master
  event:
  - push
  - pull_request
  - tag
