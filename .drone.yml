kind: pipeline
name: default

steps:
- name: build
  image: node
  commands:
  - git config user.email "609448234@qq.com"
  - git config user.name "cacivy"
  - npm install
  - npm run ci
  - npm run codecov
  - npm run deploy

trigger:
  branch:
  - master
  event:
  - push
  - pull_request
  - tag
